/** @file   dp_device_manager.cpp
 *  @brief  DeviceManager implementation.
 *
 *  The DeviceManager is mostly header-only. This TU ensures the singleton
 *  static local and vtable-related symbols are emitted in exactly one
 *  translation unit. */

#include "dp_device_manager.h"
